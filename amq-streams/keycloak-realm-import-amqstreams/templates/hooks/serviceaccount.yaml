apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "keycloak-realmimport-amqstreams.name" . }}-hook
  {{- if .Values.namespace }}
  namespace: {{ .Values.namespace }}
  {{- end }}
  labels:
    {{- include "keycloak-realmimport-amqstreams.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.argocd.enabled }}
    {{- include "keycloak-realmimport-amqstreams.argocd-syncwave" . | nindent 4 }}
    {{- else }}
    "helm.sh/hook": pre-install  
    {{- end }} 
