apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "keycloak-realmimport-console-oauth.name" . }}-hook
  {{- if .Values.namespace }}
  namespace: {{ .Values.namespace }}
  {{- end }}
  labels:
    {{- include "keycloak-realmimport-console-oauth.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.argocd.enabled }}
    {{- include "keycloak-realmimport-console-oauth.argocd-syncwave" . | nindent 4 }}
    {{- else }}
    "helm.sh/hook": pre-install  
    {{- end }} 
